# Universal media notes

Media generation is capability-driven. For a non-standard image or video API, choose **Universal HTTP API**, enable the capability, then configure its HTTP method, endpoint, request template, and response path.

Images may be returned as public URLs, data URIs, standard base64/base64url strings, or nested values under common `data`, `output`, `results`, and `artifacts` wrappers.
